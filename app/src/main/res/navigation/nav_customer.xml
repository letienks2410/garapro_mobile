<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_customer"
    app:startDestination="@id/homeFragment">

    <!-- Home -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.garapro.ui.home.HomeFragment"
        android:label="Trang chính"
        tools:layout="@layout/fragment_home" />

    <!-- 🟦 APPOINTMENT GRAPH: Appointment + Quotations + Details -->
    <navigation
        android:id="@+id/appointmentGraph"
        app:startDestination="@id/appointmentNavFragment">

        <!-- Container Fragment (TabLayout, ViewPager2, ...) -->
        <fragment
            android:id="@+id/appointmentNavFragment"
            android:name="com.example.garapro.ui.appointmentNav.AppointmentNavFragment"
            android:label="Đặt lịch"
            tools:layout="@layout/fragment_appointment_nav" />

        <!-- Appointments List -->
        <fragment
            android:id="@+id/appointmentsFragment"
            android:name="com.example.garapro.ui.appointments.AppointmentsFragment"
            android:label="Lịch hẹn"
            tools:layout="@layout/fragment_appointments">
        </fragment>

        <!-- Appointment Detail -->
        <fragment
            android:id="@+id/appointmentDetailFragment"
            android:name="com.example.garapro.ui.appointments.RepairRequestDetailFragment"
            android:label="Chi tiết"
            tools:layout="@layout/bottom_sheet_repair_request_detail">

            <argument
                android:name="repairRequestId"
                app:argType="string" />
        </fragment>


        <!-- Quotations -->
        <fragment
            android:id="@+id/quotationsFragment"
            android:name="com.example.garapro.ui.quotations.QuotationsFragment"
            android:label="Báo Giá"
            tools:layout="@layout/fragment_quotations">
        </fragment>

        <!-- Quotation Detail -->
        <fragment
            android:id="@+id/quotationDetailFragment"
            android:name="com.example.garapro.ui.quotations.QuotationDetailFragment"
            android:label="Chi tiết Báo Giá"
            tools:layout="@layout/fragment_quotation_detail">

            <argument
                android:name="quotationId"
                app:argType="string" />
        </fragment>
    </navigation>

    <!-- 🟧 REPAIR TRACKING GRAPH: List + Detail -->
    <navigation
        android:id="@+id/repairTrackingGraph"
        app:startDestination="@id/repairTrackingFragment">

        <!-- Repair Tracking List -->
        <fragment
            android:id="@+id/repairTrackingFragment"
            android:name="com.example.garapro.ui.RepairProgress.RepairProgressListFragment"
            android:label="Quá trình sửa chữa"
            tools:layout="@layout/fragment_repair_tracking">

            <action
                android:id="@+id/action_repairTrackingFragment_to_repairProgressDetailFragment"
                app:destination="@id/repairProgressDetailFragment" />
        </fragment>

        <!-- Repair Progress Detail -->
        <fragment
            android:id="@+id/repairProgressDetailFragment"
            android:name="com.example.garapro.ui.RepairProgress.RepairProgressDetailFragment"
            android:label="Chi tiết Sửa chữa"
            tools:layout="@layout/fragment_repair_progress_detail">

            <argument
                android:name="repairOrderId"
                app:argType="string" />

        </fragment>
    </navigation>

    <!-- 🟩 REPAIR ARCHIVED GRAPH: List + Detail -->
    <navigation
        android:id="@+id/repairArchivedGraph"
        app:startDestination="@id/repairArchivedFragment">

        <!-- Repair Archived List -->
        <fragment
            android:id="@+id/repairArchivedFragment"
            android:name="com.example.garapro.ui.RepairProgress.archived.RepairOrderArchivedListFragment"
            android:label="Lịch sử sửa chữa"
            tools:layout="@layout/fragment_repair_order_archived_list">

            <action
                android:id="@+id/action_repairArchivedFragment_to_repairArchivedDetailFragment"
                app:destination="@id/repairArchivedDetailFragment" />
        </fragment>

        <!-- Repair Archived Detail -->
        <fragment
            android:id="@+id/repairArchivedDetailFragment"
            android:name="com.example.garapro.ui.RepairProgress.archived.RepairOrderArchivedDetailFragment"
            android:label="Chi tiết lịch sử sửa chữa"
            tools:layout="@layout/fragment_repair_order_archived_detail">

            <argument
                android:name="repairOrderId"
                app:argType="string" />

        </fragment>
    </navigation>
    <fragment
        android:id="@+id/chat"
        android:name="com.example.garapro.ui.chat.ChatFragment"
        android:label="Chat" >
    </fragment>
    <!-- Profile -->
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.garapro.ui.profile.ProfileFragment"
        android:label="Hồ sơ"
        tools:layout="@layout/fragment_profile" />

    <!-- Vehicles -->
    <fragment
        android:id="@+id/vehiclesFragment"
        android:name="com.example.garapro.ui.vehicles.VehiclesFragment"
        android:label="Xe của tôi"
        tools:layout="@layout/fragment_vehicles" />

    <!-- Notifications -->
    <fragment
        android:id="@+id/notificationsFragment"
        android:name="com.example.garapro.ui.notifications.NotificationsFragment"
        android:label="Thông báo"
        tools:layout="@layout/fragment_notifications" />

    <!-- Global actions (vẫn dùng được, navigate tới fragment nằm trong graph con) -->
    <action
        android:id="@+id/action_global_quotationDetailFragment"
        app:destination="@id/quotationDetailFragment" />

    <action
        android:id="@+id/action_global_appointmentDetailFragment"
        app:destination="@id/appointmentDetailFragment" />

    <!-- thêm 2 cái này -->
    <action
        android:id="@+id/action_global_repairTrackingFromRequest"
        app:destination="@id/repairProgressDetailFragment" />

    <!--  mới: đi vào graph Lịch sử sửa chữa -->
    <action
        android:id="@+id/action_global_repairArchivedFromRequest"
        app:destination="@id/repairArchivedGraph" />
</navigation>
